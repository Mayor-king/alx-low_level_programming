#include "main.h"

/**
 * _memset() - more pointers
 * @s: pointer to char params
 * @b: data to change
 * @n: sfjk
 * Return: s
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	i = 0;
	while (i < n)
	{
		s[a] = b;
		i++;
	}

	return (s);
}
